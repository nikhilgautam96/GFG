package BST;
public class merge_2_BST {
    // Method 1 - Time : O(n + m), Space : O(n + m).
        // ArrayList al1 = inorder(bst1);
        // ArrayList al2 = inorder(bst1);
        // Merge(al1, al2) - al1 & al2 are already sorted.
    // Method 2 - Time : O(n + m), Space : O(n + m).
        // DLL l1 = lattenBstToSortedDll(bst1);
        // Dll l2 = flattenBstToSortedDll(bst2);
        // merge(l1, l2) : merge two sorted linked list.
}
